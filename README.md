# Krishna Payyavula — Portfolio

Personal portfolio site built with React + Vite. Hosted on Cloudflare Pages.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output goes to `dist/` folder.

## Deployment

Deployed automatically via Cloudflare Pages on every push to `main`.
